// DONT MAKE IDS DUPLICATE!

export enum MobType {
    BEE = 0,
    STARFISH = 1,
    JELLYFISH = 2,
    BEETLE = 3,
    BUBBLE = 4,
}

export enum PetalType {
    BASIC = 5,
    FASTER = 6,
    BEETLE_EGG = 7,
}