// DONT MAKE IDS DUPLICATE!

export enum MobType {
    BEE = 0,
    STARFISH = 3,
    JELLYFISH = 4,
    BEETLE = 5,
    BUBBLE = 6,
}

export enum PetalType {
    BASIC = 1,
    FASTER = 2,
    BEETLE_EGG = 7,
}